export enum WeightUnits {
  GRAM = Math.pow(10, 6),
  KG = Math.pow(10, 3),
  TONNE = 1,
}

export enum WeightUnitsString {
  GRAM = 'г.',
  KG = 'кг.',
  TONNE = 'тонн',
}

export enum TimeUnitsString {
  day = 'день',
  month = 'месяц',
  year = 'год',
}
